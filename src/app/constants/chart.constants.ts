const baseOptions = {
  responsive: true,
  maintainAspectRatio: false,
  plugins: {
    legend: { display: false },
  },
}

export const pieOptions = baseOptions

export const barOptions = baseOptions

export const lineOptions = baseOptions
